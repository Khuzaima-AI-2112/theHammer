# AI_GUIDANCE.md — moved

This file used to hold three unrelated documents stacked under a heading that
read `# AGENTS.md`. It has been split so each has one home:

| What | Now lives in |
| --- | --- |
| Project alignment rules and the Absolute Stop Protocol | `AGENTS.md` (repo root) |
| Developer guardrails, rules 1–7 | `AGENTS.md` (repo root) |
| GCP provisioning runbook (preflight, phases 1–7, troubleshooting) | `docs/gcp-setup.md` |

**Do not add rules here.** `AGENTS.md` at the repo root is the single source of
truth for how agents and developers work in this repository. This stub exists
only so that existing pointers to `.agent/AI_GUIDANCE.md` still resolve.
