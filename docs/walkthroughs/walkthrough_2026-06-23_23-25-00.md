# SRE Hardening Sprint — Walkthrough

**Date**: 2026-06-23  
**Sprint Goal**: Remediate all Critical (P0) and High (P1) findings from the SRE & Architecture Audit, plus all executable Medium (P2) findings.

---

## P0 — Critical Fixes (3/3 resolved)

| Finding | Fix | File |
|---|---|---|
| C1. Worker endpoints unauthenticated | Added `requireAdmin` to `/worker/reports` and `/worker/ocr` | `backend/src/index.js` |
| C2. `localhost` in production CORS | Gated `http://localhost:3000` behind `NODE_ENV !== 'production'` | `backend/src/index.js` |
| C3. Deprecated `API_KEY` secret injected | Removed `--set-secrets API_KEY=...` from Cloud Build | `cloudbuild.yaml` |

## P1 — High Fixes (5/5 resolved)

| Finding | Fix | File |
|---|---|---|
| H1. Portal uses backend SA | `cloudbuild.yaml` now deploys portal with `thehammer-portal-sa` | `cloudbuild.yaml` |
| H2. Dead `api_keys` lookup | Removed the block; uses `req.hammerUser.uid` directly | `backend/src/index.js` |
| H3. `module.exports` before routes | Moved to bottom of file, after all `app.use()` mounts | `backend/src/index.js` |
| H4. Backend timeout 30s | Updated to `300s` to match architecture spec | `cloudbuild.yaml` |
| H5. `min-instances: 0` on API | Updated to `1` to keep a warm instance on the capture path | `cloudbuild.yaml` |

> [!IMPORTANT]
> **Action Required**: The portal SA must be provisioned manually before the next Cloud Build deployment will succeed:
> ```bash
> gcloud iam service-accounts create thehammer-portal-sa \
>   --display-name="The Hammer Portal Service Account"
> gcloud storage buckets add-iam-policy-binding gs://thehammer-storage-2026 \
>   --member="serviceAccount:thehammer-portal-sa@thehammer.iam.gserviceaccount.com" \
>   --role="roles/storage.objectViewer"
> ```

## P2 — Medium Fixes (5/5 resolved)

| Finding | Fix | File |
|---|---|---|
| M1. Firestore errors swallowed | `firestoreWrite()` now returns error string; `/capture` includes `warning` in response | `backend/src/index.js` |
| M2. `EXTENSION_ID` missing from env | Injected `EXTENSION_ID=ndmbjlbmcdlmijglkfcelbiejmghiikm` into Cloud Build | `cloudbuild.yaml` |
| M3. `ADMIN_ORIGIN` missing from env | Injected portal Cloud Run URL into `ADMIN_ORIGIN` | `cloudbuild.yaml` |
| M4. No pagination on list endpoints | Added `limit(100)` + `startAfter` cursor pagination | `projects.js`, `users.js` |
| M5. SSRF in webhook dispatch | Added `validateWebhookUrl()` blocking private IPs, `http://`, and metadata server | `backend/src/index.js` |

## M6 — Portal Refactor (Resolved)
- Extracted 2500-line `portal/index.html` into `styles.css` and `app.js` to dramatically improve maintainability and unblock future strict CSP implementation.

## P3 — Low/Best Practice Fixes (4/4 resolved)

| Finding | Fix | File |
|---|---|---|
| L1. Swallowed / plaintext logs | Created structured JSON logger for GCP Cloud Logging | `backend/src/lib/logger.js` |
| L2. Shallow health check | Added deep Firestore ping to `/health` returning 503 if disconnected | `backend/src/index.js` |
| L3. Node 20 EOL risk | Upgraded Docker base image and engines to Node 22 Alpine | `backend/Dockerfile`, `package.json` |
| L4. Extension font CDN leak | Bundled Inter font locally via `@font-face` | `extension/popup.html`, `extension/fonts/` |

## Commits
- `2ba0702` — `security: fix P0 audit findings (CORS, worker auth, API_KEY)`
- `cb57fe9` — `refactor: fix P1 audit findings (infra and code cleanup)`
- `af87ffa` — `security: fix P2 audit findings (CORS env vars, SSRF, Firestore warnings, pagination)`
- `(Latest)` — `refactor: address P3 and M6 audit findings (logging, portal refactor, Node 22, extension fonts, deep health check)`
